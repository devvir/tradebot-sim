# Local MongoDB (source of tradeBin1m/5m for extract-bins). Host-mapped port.
DB_USER=
DB_PASS=
DB_PORT=

# Data root (extracted CSVs, parquet, bins, runs archive) and vault buckets.
# Defaults live in src/config.ts; override here if the data moves.
# POC_DIR=/storage/bitmex/scalper-poc
# VAULT_DIR=/storage/bitmex/vault
